# Bomber-Pengu-Revive
The original game is back !

## Lancer le jeu

### Windows
1. Double-cliquez sur `start.bat`
2. Ouvrez votre navigateur sur `http://localhost:8080`

### Linux/Mac
1. Exécutez `./start.sh` (ou `bash start.sh`)
2. Ouvrez votre navigateur sur `http://localhost:8080`

Le serveur TCP tourne sur le port 6897 et le serveur HTTP sur le port 8080.
